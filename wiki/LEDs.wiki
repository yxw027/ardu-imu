= LED displays = 

When AIRSTART = 0, the red, blue and yellow LEDs will flash while the IMU is calibrating at startup. 

ArduIMU Indicator LEDs (after startup):

  * Green LED = Power On
  * Yellow LED = On in AIRSTART mode; off if not
  * Blue LED = GPS lock
  * Red LED = Saturated Sensors